package
class {
public static void main() {
String s = "Himanshu";
String reverse = "";

int v = 0
convert them in lowercase s.tolowercase()

for(int i = s.length()-1;i>=0;i--)
        {
            reverse = reverse + str.charAt(i);
        }

sop(reverse)
for(int i= 0;i<s.length();i++) 
{ 
ch = s.charAt(i);
if(ch=='a'||ch =='e'||ch=='i'||ch=='o'||ch=='u') 
{v++; 
} }
        sop(vcount);}
}
}